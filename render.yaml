services:
  - type: web
    name: the-poizon-bot
    env: python
    plan: free
    buildCommand: ""
    startCommand: "uvicorn main:app --host 0.0.0.0 --port 10000"
    envVars:
      - key: BOT_TOKEN
        sync: false
      - key: DOMAIN
        value: https://poizon-5ih7.onrender.com  # замени на свой домен
    autoDeploy: true
